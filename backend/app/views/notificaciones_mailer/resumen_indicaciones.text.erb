<%= @club.name %> — Indicaciones médicas
<%= Date.today.strftime('%d/%m/%Y') %>

Hay <%= @total %> indicación<%= @total == 1 ? '' : 'es' %> que requieren atención.

<% if @vencidas.any? %>
VENCIDAS:
<% @vencidas.each do |ind| %>
- <%= ind.paciente.nombre_completo %> — <%= ind.patologia %> (venció <%= ind.fecha_vencimiento.strftime('%d/%m/%Y') %>)
<% end %>
<% end %>

<% @por_vencer.each do |dias, lista| %>
<% next if lista.empty? %>
VENCEN EN <%= dias %> DÍAS:
<% lista.each do |ind| %>
- <%= ind.paciente.nombre_completo %> — <%= ind.patologia %> (<%= ind.fecha_vencimiento.strftime('%d/%m/%Y') %>)
<% end %>
<% end %>

Ver en el sistema: <%= ENV.fetch('APP_HOST', 'https://app.clubcultivo.app') %>/pacientes
